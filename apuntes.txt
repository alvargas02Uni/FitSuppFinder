use fitsuppfinder;

DROP TABLE productsHSN CASCADE;

CREATE TABLE productsHSN (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    brand VARCHAR(255) NOT NULL,
    price DECIMAL(10,2) NOT NULL,
    quantity FLOAT,
    count INT,
    measure VARCHAR(10),
    img_source VARCHAR(255)
);

SELECT * FROM productsHSN WHERE name="HARINA DE COCO";

CREATE TABLE productsProzis (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    brand VARCHAR(255) NOT NULL,
    price DECIMAL(10,2) NOT NULL,
    quantity FLOAT,
    count INT,
    measure VARCHAR(10),
    img_source VARCHAR(255)
);

DROP TABLE productsMyProtein CASCADE;

CREATE TABLE productsMyProtein (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    brand VARCHAR(255) NOT NULL,
    price DECIMAL(10,2) NOT NULL,
    quantity FLOAT,
    count INT,
    measure VARCHAR(10),
    img_source VARCHAR(255)
);

podman run -ti nginx /bin/bash